# Rust
/target/
**/*.rs.bk

# IDE
.idea/
.vscode/
*.swp

# Data
/data/

# OS
.DS_Store

# TLS material (device-trusted cert/key, never commit)
/certs/

# Local sync data (real notebooks) and device DB - never commit
/test-storage/
/remarkable-storage/
/crash-dumps/

# Auth-token signing secret (generated on first start) - never commit
jwt_secret

# Cross-compile output
/target-linode/
rm-proxy/target/

# Local dependency overrides (see SETUP.md)
/.cargo/config.toml
