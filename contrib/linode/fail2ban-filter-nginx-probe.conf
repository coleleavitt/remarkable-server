# /etc/fail2ban/filter.d/nginx-probe.conf
# Requests nginx dropped with 444 (/.env, /.git, wp-*, *.php, ...). Nobody legit hits these.
[Definition]
failregex = ^<HOST> \S+ \S+ \[[^\]]+\] "[^"]*" 444 
ignoreregex =
datepattern = {^LN-BEG}%%d/%%b/%%Y:%%H:%%M:%%S %%z
